#' Add together two numbers.
#'
#' @param x A number.
#' @param y A number.
#' @param z A number.
#' @export
#' @return The sum of \code{x} and \code{y} and \code{z}.
#' @examples
#' add(1, 1,2)
#' add(10, 1,3)
add <- function(x, y,z) {
  x + y+z
}

